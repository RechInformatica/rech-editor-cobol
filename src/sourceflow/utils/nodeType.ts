/**
 * Tipo de nodos para utilizar no COBOL Flow
 */
export enum NodeType {
    Command = 0,
    Paragraph = 1,
    Method = 2,
    If = 3,
    Else = 4,
    When = 5,
    PerformLoop = 6,
    PerformThru = 7,
    Evaluate = 8
}
